import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const SeatCellComponent = (props: Props) => {
  return (
    <View>
      <Text>SeatCellComponent</Text>
    </View>
  );
};

export default SeatCellComponent;

const styles = StyleSheet.create({});
